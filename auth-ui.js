(function(){function heartbeat(){let sid=localStorage.getItem('flarePresenceId');if(!sid&&window.crypto?.getRandomValues){const b=new Uint8Array(16);crypto.getRandomValues(b);sid=[...b].map(x=>x.toString(16).padStart(2,'0')).join('');localStorage.setItem('flarePresenceId',sid)}if(!sid)return;const ping=()=>fetch('/api/online',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({sid}),keepalive:true}).catch(()=>{});ping();setInterval(ping,20000);window.addEventListener('pagehide',ping)}function init(){heartbeat()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();
