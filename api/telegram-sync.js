export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const host=req.headers['x-forwarded-host']||req.headers.host;if(!host)throw new Error('Host header is missing');const protocol=req.headers['x-forwarded-proto']||'https';const r=await fetch(`${protocol}://${host}/api/telegram?refresh=1`,{headers:{Accept:'application/json'},cache:'no-store'});const text=await r.text();res.status(r.status);res.setHeader('Content-Type','application/json; charset=utf-8');return res.send(text)}catch(e){return res.status(502).json({error:'Telegram sync failed',message:String(e.message||e)})}}